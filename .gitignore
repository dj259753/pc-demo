node_modules/
.DS_Store

# 大型模型文件（onnx / bin）不上传
models/
*.onnx
*.bin

# tools 里的构建产物（wasm/ruffle 等）
tools/*.wasm
tools/core.ruffle.*.js
tools/ruffle.js

# Electron 构建产物
dist/
release/*.zip
# Gateway 资源可提交；嵌入式 Node 单文件 > GitHub 100MB，由 CDN / npm run ensure:runtime-node 拉取
resources/targets/**/runtime/node
resources/targets/**/runtime/node.exe
# 若仍从 QQClaw 同步见 npm run prepare:gateway